# Sample artefacts
#
# `canonical.parquet` is regenerated by `scripts/prepare_samples.py`.
# The live OpenSky JSON in data/raw/ is a real column-schema snapshot (Zagreb FIR).
# `raw_unknown.csv` is the unseen-schema rehearsal file.
